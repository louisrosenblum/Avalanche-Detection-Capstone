% Will put everything printed into the command window into a .txt file 
% to be used in calculating an average error.
diary('log.txt')

result = []
probs = []

mean_result = []
mean_prob = []
tic

for snr = 8:30
    
for go = 1:10
    go
    simulation_3D();
    probs(go) = prob;
    result(go) = error;
end

mean_result(snr-7) = mean(result);


end

diary('off')

toc

